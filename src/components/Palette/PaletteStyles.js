import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
   Palette: {
      height: '100vh',
      display: 'flex',
      flexDirection: 'column',
      overflow: 'hidden'
   },
   colors: {
      height: '90%',
      display: 'flex',
      flexWrap: 'wrap'
   }
})